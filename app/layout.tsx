import './globals.css';
import Link from 'next/link';
export const metadata={title:'BakuSafeMap | Безопаснее вместе',description:'Карта дорожных опасностей и проблем Баку'};
export default function RootLayout({children}:{children:React.ReactNode}){return <>{children}</>}
